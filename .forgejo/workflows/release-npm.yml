name: Publish to npm

on:
  push:
    branches: [main, beta]
  workflow_dispatch: {}

jobs:
  publish:
    uses: Conduction/.github/.forgejo/workflows/release-npm.yml@main
    with:
      node-version: "24"
    secrets: inherit
