name: Publish to npm

on:
  push:
    branches: [main, beta]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run semantic-release --dry-run only"
        type: boolean
        default: false

jobs:
  publish:
    uses: Conduction/.github/.forgejo/workflows/release-npm.yml@main
    with:
      node-version: "24"
    secrets: inherit
