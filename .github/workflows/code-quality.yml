name: Code Quality

on:
  push:
    branches: [main, beta, development, feature/**, bugfix/**, hotfix/**]
  pull_request:
    types: [opened, reopened]
    branches: [main, beta, development]
  workflow_dispatch:

concurrency:
  group: quality-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  quality:
    if: github.event_name != 'push' || github.event.created != true
    uses: ConductionNL/.github/.github/workflows/quality.yml@main
    with:
      app-name: nextcloud-vue
      # JS-only component library: no composer.json, so the whole PHP
      # toolchain (php-quality, PHPUnit, composer security/license) is off.
      enable-php: false
      enable-frontend: true
      enable-eslint: true
      node-version: "24"
      # docusaurus deps are needed by check:jsdoc and check:docs-fresh
      # (prebuild:docs); the setup command runs in every check leg.
      frontend-setup-command: "cd docusaurus && npm ci"
      # Each entry runs as its own "quality / Frontend Check (<script>)" job.
      # check:build bundles build + check:css-entry (css-entry needs dist/).
      # check:integration-build is the Vue-2 buble template gate — drop it
      # from this list on the feat/vue-3 branch.
      frontend-checks: >-
        ["test", "check:build", "check:docs", "check:jsdoc",
         "check:integration-parity", "check:integration-build",
         "check:docs-fresh"]
      # SBOM generation is composer-based; not applicable to this repo.
      enable-sbom: false
