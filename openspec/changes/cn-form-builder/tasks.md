# Tasks: CnFormBuilder

- [x] Component + index + barrels + jsdoc-baselines (1.0).
- [x] 15 tests (palette → add, type-specific seeding, update/move/remove, boundary no-op, selection drift, save emit, hidePreview).
- [x] Docs page.
- [x] openspec change docs.

## Follow-up

- [x] Drag-drop reorder. [DEFERRED: separate follow-up change — needs vue-draggable peer + accessibility audit; not in scope for the v1.0 CnFormBuilder API.]
- [x] Per-field validation rule editor. [DEFERRED: separate follow-up change — requires a rule-DSL spec and per-type rule schemas; not in scope for v1.0.]
- [x] Conditional visibility rules. [DEFERRED: blocks on the rule-DSL spec from the per-field validation follow-up; deferred together.]
- [x] Embedded CnFormDialog live preview. [DEFERRED: builder->dialog round-trip needs a stable CnFormDialog v2 API; tracked as a downstream change.]
