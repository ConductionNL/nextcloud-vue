# resolveFlowNodeEditor

The lookup `CnFlowDetail` performs when a node edit opens: returns the component an app registered for the node type, or `null` — which means the generic [`CnFlowNodeEditModal`](../components/cn-flow-node-edit-modal.md). See [registerFlowNodeEditor](./register-flow-node-editor.md) for the registry and the contract.
