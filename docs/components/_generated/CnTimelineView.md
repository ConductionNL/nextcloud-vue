<!-- AUTO-GENERATED by vue-docgen-cli — do not edit. Source: src/components/CnTimelineView/CnTimelineView.vue -->

### Props

| Name            | Type                                                                                                           | Required | Default                  | Description                                                                                                                                                                                                                    |
| --------------- | -------------------------------------------------------------------------------------------------------------- | -------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `events`        | `Array<{id?:string,title?:string,start:string,end?:string,location?:string,description?:string,kind?:string}>` |          | `[]`                     | Events to render. Each entry: `{ id?, title?, start, end?, location?, description?, kind? }`. `start` MUST parse to a valid Date.                                                                                              |
| `title`         | `string`                                                                                                       |          | `''`                     | Optional title rendered above the timeline.                                                                                                                                                                                    |
| `description`   | `string`                                                                                                       |          | `''`                     | Optional description rendered under the title.                                                                                                                                                                                 |
| `emptyLabel`    | `string`                                                                                                       |          | `'No events scheduled.'` | Empty-state label when events[] is empty.                                                                                                                                                                                      |
| `untitledLabel` | `string`                                                                                                       |          | `'Untitled'`             | Fallback label for events without a title.                                                                                                                                                                                     |
| `hideCounts`    | `boolean`                                                                                                      |          | `false`                  | Hide the per-group event count badge.                                                                                                                                                                                          |
| `groupBy`       | `(event: object) => { key: string, label: string }`                                                            |          | `null`                   | Custom grouping function. Receives an event; returns `{ key, label }` for the group it belongs to. Default groups by ISO date (YYYY-MM-DD) with a locale `toLocaleDateString` label.                                           |
| `sort`          | `string`                                                                                                       |          | `'asc'`                  | Sort direction for groups: `'asc'` (oldest first) or `'desc'` (newest first).                                                                                                                                                  |
| `kindClassMap`  | `Record<string,string>`                                                                                        |          | `\{\}`                   | Optional `kind` → CSS-class-suffix map. Each event's `kind` becomes `cn-timeline-view__event--&lt;class&gt;` so consumers can colour-code (`session`, `break`, `holiday`, etc.). When omitted no kind-specific class is added. |
| `locale`        | `string`                                                                                                       |          | `undefined`              | Locale string passed to `Date.toLocaleDateString` for the default group label. Defaults to the browser locale.                                                                                                                 |

### Events

| Name          | Payload | Description                                                       |
| ------------- | ------- | ----------------------------------------------------------------- |
| `event-click` | —       | Emitted on event row click. Payload is the original event object. |

### Slots

| Name     | Bindings                  | Description                                                                   |
| -------- | ------------------------- | ----------------------------------------------------------------------------- |
| `header` | —                         | header Replaces the default title + description block in the timeline header. |
| `event`  | `event`, `formatted-time` | event Per-event body. Scope: \{ event,                                        |
