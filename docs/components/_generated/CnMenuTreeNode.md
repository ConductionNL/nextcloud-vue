<!-- AUTO-GENERATED by vue-docgen-cli — do not edit. Source: src/components/CnMenuWidget/CnMenuTreeNode.vue -->

### Props

| Name                | Type                                      | Required | Default       | Description                                                |
| ------------------- | ----------------------------------------- | -------- | ------------- | ---------------------------------------------------------- |
| `item`              | `object`                                  | ✓        | —             | Single menu item `{label, url, icon, children}`.           |
| `depth`             | `number`                                  |          | `1`           | 1-indexed depth — drives indentation.                      |
| `showIcons`         | `boolean`                                 |          | `true`        | When false, all icon slots collapse to invisible spacers.  |
| `expandedByDefault` | `boolean`                                 |          | `false`       | When true, every node mounts already expanded.             |
| `activePath`        | `Record<string, 'active'&#124;'in-path'>` |          | `\{\}`        | Dotted-key map of `'active' \| 'in-path'` flags.           |
| `activeLeafKey`     | `string`                                  |          | `null`        | Key of the deepest active leaf; used to disambiguate ties. |
| `currentKey`        | `string`                                  | ✓        | —             | Key of THIS node within the parent's path map.             |
| `activeHighlight`   | `string`                                  |          | `'underline'` | Highlight style — drives the row class.                    |

### Events

| Name       | Payload | Description                                               |
| ---------- | ------- | --------------------------------------------------------- |
| `navigate` | —       | Fired when a leaf is activated; payload is the menu item. |
                                                                                                                                                                                                                                                                                                  |
| `maxDepth` | `number`                                |          | `1`     | Maximum nesting depth that may gain children (CnAppNav supports one level).                                                                                                                                                                                                                                                                                                                                                                                         |
| `pages`    | `Array<{value: string, label: string}>` |          | `[]`    | Selectable target pages for the Route field, as `{ value: route, label }` options (derived from the manifest's `pages[]`). Passed down recursively so child rows offer the same pages.                                                                                                                                                                                                                                                                              |
| `section`  | `union`                                 |          | `null`  | Which nav section this editor scopes to at the TOP level. CnAppNav splits `menu[]` by `item.section` (`main` \| `footer` \| `settings`): `"settings"` shows only the gear-foldout items; anything else (the default) shows the non-settings items. Lets the main menu editor and the settings-menu editor share this component over one `menu[]` array without showing each other's items. Only filters depth 0 — children (nested via `children[]`) always render. |
