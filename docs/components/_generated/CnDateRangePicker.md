<!-- AUTO-GENERATED by vue-docgen-cli — do not edit. Source: src/components/CnDateRangePicker/CnDateRangePicker.vue -->

### Props

| Name          | Type                                          | Required | Default                                                           | Description                                                                                                                                                                |
| ------------- | --------------------------------------------- | -------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `value`       | `{ from: string, to: string, preset: string } | null`    |                                                                   | `null`                                                                                                                                                                     | Current range value. `from` / `to` are ISO-8601 UTC strings; `preset` is the id of the preset that produced the range (or `'custom'` when the user typed values by hand). |
| `presets`     | `union`                                       |          | `() =&gt; DEFAULT_DATE_RANGE_PRESETS.map((p) =&gt; (\{ ...p \}))` | Preset list. Each entry: `{ id, label, days }`. Use `days: null` to mark a preset as manual (e.g. the canonical `custom` entry). Defaults to `DEFAULT_DATE_RANGE_PRESETS`. |
| `disabled`    | `boolean`                                     |          | `false`                                                           | When `true`, both pickers AND the preset select are disabled. Mirrors `NcDateTimePicker.disabled`.                                                                         |
| `dateFormat`  | `string`                                      |          | `'YYYY-MM-DD'`                                                    | Date input visual format string. Forwarded to the underlying `NcDateTimePicker`s. Default: `'YYYY-MM-DD'`.                                                                 |
| `presetLabel` | `string`                                      |          | `() =&gt; t('nextcloud-vue', 'Range preset')`                     | Accessible label for the preset dropdown.                                                                                                                                  |
| `fromLabel`   | `string`                                      |          | `() =&gt; t('nextcloud-vue', 'From')`                             | Accessible label for the start-of-range picker.                                                                                                                            |
| `toLabel`     | `string`                                      |          | `() =&gt; t('nextcloud-vue', 'To')`                               | Accessible label for the end-of-range picker.                                                                                                                              |

### Events

| Name    | Payload | Description |
| ------- | ------- | ----------- |
| `input` | —       |             |
