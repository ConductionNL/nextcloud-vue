<!-- AUTO-GENERATED by vue-docgen-cli — do not edit. Source: src/components/CnContextMenu/CnContextMenu.vue -->

### Props

| Name          | Type                                                                      | Required                   | Default                 | Description                                                                                                                                                                                                                                                                                                                          |
| ------------- | ------------------------------------------------------------------------- | -------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `open`        | `boolean`                                                                 |                            | `false`                 | Whether the context menu is open. Use with `.sync` modifier. Bind to `useContextMenu().isOpen`.                                                                                                                                                                                                                                      |
| `actions`     | `Array<{label: string, icon: object, handler: Function, disabled: boolean | Function, visible: boolean | Function, title: string | Function, destructive: boolean}>`                                                                                                                                                                                                                                                                                                    |        | `[]`                                                                                                                                                                                                                                      | Action definitions rendered as NcActionButton items in the default panel. Same format as CnRowActions: `{ label, icon?, handler?, disabled?, visible?, title?, destructive? }`. `visible` (boolean \| (targetItem) =&gt; boolean) hides the entry when falsy (default: shown). `title` (string \| (targetItem) =&gt; string) renders as a native tooltip — useful for explaining why an entry is disabled. When the entire array is empty (or all entries are filtered out), only the default slot content is rendered. |
| `targetItem`  | `object                                                                   | string                     | number`                 |                                                                                                                                                                                                                                                                                                                                      | `null` | The right-clicked item (row, folder, etc.). Passed to action `handler` and `disabled` callbacks, included in the `action` event payload, and forwarded to custom panel slot scope as `targetItem`. Bind to `useContextMenu().targetItem`. |
| `activePanel` | `string`                                                                  |                            | `null`                  | Name of the currently active custom panel, or `null` for the default NcActions action list. When set, the matching `#panel:<name>` slot is rendered in place of the action list. Use with `.sync` to let panel slots call `back()` to clear it. Closing the menu auto-resets to `null` so the next open starts on the default panel. |

### Events

| Name                 | Payload | Description                                                                                                                                      |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `update:open`        | —       |                                                                                                                                                  |
| `action`             | —       |                                                                                                                                                  |
| `update:activePanel` | —       |                                                                                                                                                  |
| `close`              | —       | Emitted when the menu closes — fired for any close cause: clicking an action, clicking outside, clicking the panel backdrop, or pressing Escape. |

### Slots

| Name                   | Bindings                               | Description                                                  |
| ---------------------- | -------------------------------------- | ------------------------------------------------------------ |
| `default`              | —                                      | default Custom NcActionButton-family content for the default |
| `panel:${activePanel}` | `name`, `back`, `close`, `target-item` | panel:&lt;name&gt; Free-form custom panel content shown when |
